# Server (required — see server.ts)
HOSTNAME=localhost
PORT=3000

# Optional: allowed browser origin for Socket.IO (defaults to http://HOSTNAME:PORT)
# Set when users open the app by IP, e.g. APP_ORIGIN=http://192.168.1.50:3000
# APP_ORIGIN=http://localhost:3000

# MySQL (required — see src/lib/db.ts)
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=
